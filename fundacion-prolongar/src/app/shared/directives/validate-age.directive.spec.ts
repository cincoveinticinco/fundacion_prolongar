import { ValidateAgeDirective } from './validate-age.directive';

describe('ValidateAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateAgeDirective();
    expect(directive).toBeTruthy();
  });
});
