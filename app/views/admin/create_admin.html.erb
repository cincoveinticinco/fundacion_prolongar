<% if !@user_admin.blank? %>
	<h1 class="text-center text-dark-orange text-uppercase">Editar Adminstrador</h1>
	<div class="container br-register"></div>
<% else %>
	<h1 class="text-center text-dark-orange text-uppercase">Crear Adminstrador</h1>
	<div class="container br-register"></div>
<% end %>

<%= form_tag({:controller => "admin", :action => "insert_admin"},:method => "post", :multipart => true, :class =>'') do %>
	<div class="container">
		<div class="row">
			<div class="col">
				<input type="hidden" name="id" value ="<%= !@user_admin.blank? ?  @user_admin.id : '' %>" >
				<label>Usuario</label>
				<input type="text" class="form-control" name="name" required="" value="<%= !@user_admin.blank? ?  @user_admin.name : '' %>">
			</div>
			<div class="col">
				<label >Contraseña</label>
				<input type="password" class="form-control" placeholder="Contraseña" id="password" name="password" <% if @user_admin.blank? %> required <%end%>>
			</div>
		</div>
					
		<div class="row">
			<div class="col">
				<label class="mt-3">Confirmar Contraseña</label>
				<input type="password" class="form-control" placeholder="Confirmar Contraseña" id="confirm_password" <% if @user_admin.blank? %> required <%end%>>
			</div>
		</div>

		 <div class="d-flex justify-content-center mt-3">
             <input type="submit" value="Guardar" class="btn-create">
        </div>
		
	</div>

<%end%>